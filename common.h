#ifndef _COMMON_H
#define _COMMON_H
#include <stdio.h>
#include <stdbool.h>
#include <math.h>

#define EPS 0.001

void clearInputBuffer();
bool isZero(double n);
bool isEqual(double a, double b);

#endif
